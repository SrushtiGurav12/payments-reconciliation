Build a small Python solution for a month-end payments reconciliation assessment. Generate deterministic synthetic data for January 2024 with these planted cases: one transaction that settles in February after a 1-2 day delay, one rounding issue that only appears when matching totals in aggregate, one duplicate row in the bank settlement dataset, and one refund that has no original transaction in the platform dataset. Use clear assumptions, pure Python if possible, add tests that prove each planted case is detected, and create a runnable entrypoint that prints and saves a reconciliation report showing matches, timing differences, duplicates, orphan refunds, missing settlements, and aggregate rounding variance.
